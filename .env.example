# ============================================
# Attendance System - Environment Variables
# ============================================
# 
# ⚠️ SECURITY WARNING:
# - NEVER commit this file with real values to git
# - Generate strong random secrets using: openssl rand -base64 32
# - Keep SUPABASE_SERVICE_ROLE_KEY secret (never expose to client)
# - Change all default credentials before production
# ============================================

# Database Configuration (Supabase)
# Get these from: https://app.supabase.com/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# 🔒 CRITICAL: Keep this secret! Never expose to client-side code
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# JWT Configuration
# Generate with: openssl rand -base64 32
# Minimum 32 characters required
JWT_SECRET=your-super-secret-jwt-key-min-32-characters-long

# Session Configuration (optional, uses JWT_SECRET if not set)
SESSION_SECRET=your-session-secret-min-32-characters-long

# Application Configuration
NEXT_PUBLIC_APP_NAME="Attendance System"
NEXT_PUBLIC_APP_URL=http://localhost:3000
PORT=3000

# Face Recognition Settings
NEXT_PUBLIC_FACE_MATCH_THRESHOLD=0.6
NEXT_PUBLIC_ENABLE_FACE_RECOGNITION=true

# Attendance Settings
NEXT_PUBLIC_CHECK_IN_RADIUS=100
NEXT_PUBLIC_WORK_START_TIME=08:00
NEXT_PUBLIC_WORK_END_TIME=17:00
NEXT_PUBLIC_LATE_THRESHOLD_MINUTES=15

# Email Configuration (Optional - for notifications)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM_EMAIL=noreply@yourcompany.com
SMTP_FROM_NAME="Attendance System"

# Upload Configuration
NEXT_PUBLIC_MAX_FILE_SIZE=5242880
NEXT_PUBLIC_ALLOWED_FILE_TYPES=image/jpeg,image/png,image/jpg

# Session Configuration
SESSION_TIMEOUT=480
MAX_LOGIN_ATTEMPTS=5
LOCKOUT_DURATION_MINUTES=15

# Development/Production
NODE_ENV=development
